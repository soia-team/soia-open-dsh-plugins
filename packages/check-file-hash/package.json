{
  "name": "soia-dsh-tool-check-file-hash",
  "version": "0.1.0",
  "private": false,
  "description": "DSH host tool that hashes files or directories with sha256 and optionally writes a JSON evidence record, so a receipt's claimed artifact can be checked against the actual bytes.",
  "license": "MIT",
  "type": "module",
  "main": "lib/index.js",
  "types": "lib/types/index.d.ts",
  "exports": {
    ".": {
      "types": "./lib/types/index.d.ts",
      "default": "./lib/index.js"
    }
  },
  "files": [
    "lib/index.js",
    "lib/types/**/*.d.ts",
    "cordis.patch.yml"
  ],
  "engines": {
    "node": ">=22.6"
  },
  "scripts": {
    "build:types": "tsc -p tsconfig.json"
  },
  "dsh": {
    "bundle": {
      "patch": "./cordis.patch.yml"
    },
    "compatibility": {
      "dsh": ">=0.1.0-rc.8 <0.2.0"
    },
    "tokenBudget": {
      "resident": 125,
      "note": "tool block only (499 chars ≈ 125 tokens); this package adds no prompt section; measured by scripts/token-budget.mjs (4 chars ≈ 1 token)"
    }
  },
  "peerDependencies": {
    "@deepseek-ai/cordis": "^4.0.2",
    "@deepseek-ai/dsh-tools": "0.1.5-rc.1 || 0.1.5-rc.2 || 0.1.6-alpha.1 || 0.1.6-alpha.2"
  },
  "devDependencies": {
    "@deepseek-ai/cordis": "^4.0.2",
    "@deepseek-ai/dsh-tools": "0.1.5-rc.1 || 0.1.5-rc.2 || 0.1.6-alpha.1 || 0.1.6-alpha.2"
  }
}
